import React from 'react';

/**
 * # DragRegion
 * Hidden region at top of app to drag it around
 */
const DragRegion: React.FunctionComponent = () => {
    return (
        <div className="drag-region" />
    )
}

export default DragRegion;